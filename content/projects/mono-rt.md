---
title: "mono-rt"
description: "Dynamic Rust bindings to the Mono runtime for injected tooling and runtime inspection."
status: "active"
weight: 100
categories: ["Runtime instrumentation"]
tags: ["rust", "mono", "runtime", "instrumentation", "bindings"]
---

mono-rt provides dynamic Rust bindings to the Mono runtime for injected tooling and runtime inspection.

It supports tooling that needs to discover, call into, or inspect Mono-hosted processes without relying on static runtime bindings.

{{< button href="https://github.com/zeishr/mono-rt" target="_blank" >}}repository{{< /button >}}
